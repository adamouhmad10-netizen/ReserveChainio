# dependencies
node_modules/
.pnpm-store/

# builds
.next/
out/
dist/
build/
artifacts/
cache/
typechain-types/
coverage/
coverage.json
.expo/

# runtime data (demo SQLite database — recreated and reseeded automatically)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# environment — never commit real secrets
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
*.pem
.vscode/
.idea/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# Confidential — Master Developer Instructions are "Developer Use Only" and
# must never be published. This repository is public; keep the PDF local only.
*ReserveChain_Final_Master_Developer_Instructions*.pdf

# Throwaway ad-hoc QA capture scripts (visual-qa.mjs is the kept, reusable one)
scripts/diag.mjs
scripts/shot.mjs
scripts/shot-hero.mjs
scripts/shot-m.mjs
scripts/shots.mjs
scripts/qa-home.mjs

# TypeScript incremental build cache
*.tsbuildinfo
